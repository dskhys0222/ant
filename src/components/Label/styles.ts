import { css } from "../../../styled-system/css";

const styles = {
  label: css({
    display: "block",
    fontWeight: 500,
    marginBottom: "0.25rem",
    marginTop: "1rem",
    color: "#222",
  }),
};

export default styles;
