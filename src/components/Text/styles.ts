import { css } from "../../../styled-system/css";

const styles = {
  text: css({
    marginBottom: "0.5rem",
  }),
  small: css({
    fontSize: "0.875rem",
    fontWeight: 400,
  }),
  medium: css({
    fontSize: "1rem",
    fontWeight: 500,
  }),
  large: css({
    fontSize: "1.25rem",
    fontWeight: 700,
  }),
};

export default styles;
